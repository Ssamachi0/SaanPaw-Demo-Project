{
  "name": "saanpaw",
  "version": "0.1.0",
  "private": true,
  "description": "SaanPaw capstone monorepo (mobile + backend + design docs).",
  "workspaces": [
    "shared",
    "backend",
    "mobile",
    "web"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:mobile": "npm --workspace mobile run start",
    "dev:console": "npm --workspace web run dev",
    "web:mobile": "npm --workspace mobile run web",
    "lint": "npm --workspace backend run lint",
    "typecheck": "npm --workspace shared run typecheck && npm --workspace mobile run typecheck && npm --workspace web run typecheck",
    "test": "npm --workspace backend run test"
  },
  "//overrides": "Many Expo/React Navigation packages accept a wide react-native peer range, so npm hoists the newest release to the repo root while the workspace pins the Expo SDK 54 version. Metro then resolves the hoisted copy and fails on changed package exports. Pinning here keeps exactly one react-native/react in the tree.",
  "overrides": {
    "react-native": "0.81.5",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
