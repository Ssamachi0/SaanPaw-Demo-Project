# Not read by anything yet.
#
# The front-end currently runs entirely off the in-memory store in
# @saanpaw/shared, so no API URL is needed. This is here as the agreed variable
# name for the backend phase - copy to .env and point it at your local API once
# the REST layer exists.
EXPO_PUBLIC_API_URL=http://localhost:4000/api/v1
